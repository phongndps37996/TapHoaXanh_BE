export enum TUserRole {
  ADMIN = 'ADMIN',
  USER = 'USER',
}
export enum loginType {
  GOOGLE = 'GOOGLE',
  EMAIL = 'EMAIL',
}
export enum TokenActionType {
  VERIFY_EMAIL = 'verify',
  RESET_PASSWORD = 'reset',
}
